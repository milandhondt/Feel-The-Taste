import { extendTheme } from "@chakra-ui/react";

const thema = extendTheme({
  fonts: {
    heading: "'Afacad Flux','Roboto', sans-serif",
    body: "'Afacad Flux', 'Roboto', sans-serif",
  },
});

export default thema;
